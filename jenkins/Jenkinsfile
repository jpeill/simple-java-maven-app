pipeline {
    agent any
    stages{
        stage('build stage'){
            steps{
                sh 'mvn clean package'
            }

            post {
                success {
                   archiveArtifacts artifacts: '**/target/*.jar'
                }
            }

        }
    } 
}
